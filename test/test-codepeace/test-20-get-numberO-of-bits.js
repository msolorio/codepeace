'use strict';

const chai = require('chai');
const expect = chai.expect;

const getNumberOfBits = require('../../codepeace/20-get-numberO-of-bits');

describe('getNumberOfBits', function() {
  it('should return a number', function() {
    // console.log(getNumberOfBits(5,3));
    //
    // expect(getNumberOfBits(4, 5)).to.be.a('number');
    // expect(getNumberOfBits(4, 5)).to.equal(1);
    // expect(getNumberOfBits(11, 7)).to.equal(2);
  });

  // it('should return an error if both inputs are not numbers', function() {
  //   expect(hamming('a')).to.be.an('error');
  // });

});
